# Required
ANTHROPIC_API_KEY=sk-ant-...
WANDB_API_KEY=...

# Optional (uses in-memory fallback if not set)
REDIS_URL=redis://localhost:6379

# Optional — W&B entity (auto-detected from API key if not set)
WANDB_ENTITY=

# Optional sponsors
DAILY_API_KEY=
